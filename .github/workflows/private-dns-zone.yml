name: Publish Private DNS Zone Bicep module

on:
    workflow_dispatch:
#    pull_request:
#        branches:
#            - main
#        paths:
#            - 'PrivateDnsZone/**'
#            - 'utilities.bicep'
#            - '.github/workflows/private-dns-zone.yml'
#    push:
#        branches:
#            - main
#        paths:
#            - 'PrivateDnsZone/**'
#            - 'utilities.bicep'
#            - '.github/workflows/private-dns-zone.yml'
jobs:
    private-dns-zone:
        permissions:
            contents: read
            id-token: write
        uses: ./.github/workflows/bicep-publish.yml
        secrets:
            azure-client-id: ${{ secrets.ClIENT_ID }}
            azure-tenant-id: ${{ secrets.TENANT_ID }}
            azure-subscription-id: ${{ secrets.SUBSCRIPTION_ID }}
        with:
            bicep-file-path: PrivateDnsZone/
            registry-name: crmahi
            registry-path: network/private-dns-zone:1.0
