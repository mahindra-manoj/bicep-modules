name: Publish Resource Group Lock Bicep module

on:
    workflow_dispatch:
#    pull_request:
#        branches:
#            - main
#        paths:
#            - 'ResourceGroupLock/**'
#            - 'utilities.bicep'
#            - '.github/workflows/resource-group-lock.yml'
#    push:
#        branches:
#            - main
#        paths:
#            - 'ResourceGroupLock/**'
#            - 'utilities.bicep'
#            - '.github/workflows/resource-group-lock.yml'
jobs:
    resource-group-lock:
        permissions:
            contents: read
            id-token: write
        uses: ./.github/workflows/bicep-publish.yml
        secrets:
            azure-client-id: ${{ secrets.ClIENT_ID }}
            azure-tenant-id: ${{ secrets.TENANT_ID }}
            azure-subscription-id: ${{ secrets.SUBSCRIPTION_ID }}
        with:
            bicep-file-path: ResourceGroupLock/
            registry-name: crmahi
            registry-path: management/rg-lock:1.0
